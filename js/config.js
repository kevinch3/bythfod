// Central configuration — everything tweakable lives here.
export const CONFIG = {
  API_BASE: 'http://localhost:3000/api',
  SIM_YEAR: 2099,
  COMP_PREFIX: 'BY',
  DEFAULT_SEED: 42,
  SPEEDS: [0.5, 1, 2, 4],
  POLL_ACTIVE_MS: 3000,
  POLL_IDLE_MS: 15000,
  FEEDBACK_TIMEOUT_MS: 1500,
  OFFLINE: false,
};
